name: 'hoop_analytics'
profile: hoop_analytics
version: '1.0.0'
config-version: 2

profile: 'hoop_analytics'

model-paths: ['models']
seed-paths: ['seeds']
'test-paths': ['tests']
macro-paths: ['macros']

target-path: 'target'
clean-targets: 
  - 'target'
  - 'dbt_packages'

models:
  hoop_analytics:
    staging: 
      +materialized: view
      +schema: staging
    intermediate: 
      +materialized: view
      +schema: staging 
    marts:
      +materialized: table
      +schema: marts
